### Example `config.yaml` for MCM

Here is an example of a config.yaml file used for a standalone installation of Common Services with an MCM Hub

[config.yaml]({{ site.static_file }}/mcm/samples/values.yaml)

